# Profile: ENVIRONMENT_TOOLCHAIN V1.0

Modes: `HOST_AUDIT`, `L0_LOCK`, `RUNTIME_CANDIDATE`, `CROSS_PLATFORM_REPRODUCTION`.

Separate exact project Authority from host evidence and compatibility observations. Prefer version files, package-manager declarations, and lock files. Missing tools are classified as environment gaps unless the Task authorizes installation. Never infer a product runtime from another project.

Acceptance records exact versions, source/installation authority, current-shell verification, new-shell reproducibility, lock strategy, platform variance, gaps, and what remains not locked.
