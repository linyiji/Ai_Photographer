# AI I/O orchestration V1 evidence

- `authority-checksums.json` records the supplied ZIP digest and the package-internal SHA-256 result.
- `inventory-map.json` records accepted contracts reused by this baseline and the new infrastructure seam.
- `verification.json` records deterministic automated verification. It is refreshed at task close.

No provider credential, image byte, raw video, frame stream, provider-native response or real provider execution is present in this evidence directory.
