# Desktop Golden Flow

Built H5 was served at `http://127.0.0.1:4173` against the real Main FastAPI/SQLite process at `http://127.0.0.1:8000`.

```text
START → SHOOT = PASS
Main Session preparation = PASS
Confirmed binary Capture → QA = PASS
QA ACCEPT → REALITY_PLUS = PASS
ACCEPT_REALITY_PLUS → FINE_TUNE = PASS
ALL brightness adjustment = PASS
Undo / Redo / Reset = PASS
Non-neutral Finalize = PASS
FINAL surface = PASS
Browser fatal errors = 0
```

Fresh desktop Session `session-5e07aedf08f4` persisted one recipe with `semantic_edit_allowed=false`, one `FINE_TUNE_DERIVED` asset, Worker/OffscreenCanvas render provenance, and a MyFinalPhoto linked to the exact recipe and capture → Reality+ → derived lineage.
