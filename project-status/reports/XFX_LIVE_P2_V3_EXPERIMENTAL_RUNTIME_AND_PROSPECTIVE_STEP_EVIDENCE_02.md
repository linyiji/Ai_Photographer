# XFX_LIVE_P2_V3_EXPERIMENTAL_RUNTIME_AND_PROSPECTIVE_STEP_EVIDENCE_02

Status: `MANUAL_REVIEW_REQUIRED / READY_FOR_OPPO_EXPLORATORY_GATE`

Update: first OPPO FRAMING_ONLY attempt is a pre-fix FAIL diagnostic, not an accepted device trial. It exposed a V3-only visual presentation omission and repeated NO_EFFECT cue issuance. A bounded same-task fix restores the existing stabilized visual boxes, adds a causal duplicate-retry barrier, clarifies copy, and extends scalar position telemetry. Post-fix verification is 231/231 automated PASS, TypeScript PASS, Build PASS / 35 modules. Fresh OPPO rerun remains required.

Git admission passed at `48ecd0c90ea304c8ce6b2198306c9060a1c576ca`, remote equal, ahead/behind `0/0`, clean worktree. No reset, rebase, force push, develop merge, Main integration, or new branch was performed.

## Authority correction

The previous Counterfactual Promotion Gate `FAIL / SOURCE_REQUIRED` is preserved. Its prior interpretation as a V3 design failure is superseded. Zero exact effectiveness comparator records establish only that historical V2 human responses cannot measure V3 human-step effectiveness.

`V3_ARCHITECTURE_DESIGN=PASS`, `V3_PURE_CONTROLLER=PASS`, `V3_STRUCTURAL_REPLAY_GATE=PASS`, `V3_EFFECTIVENESS_EVIDENCE=SOURCE_REQUIRED`, and `V3_PRODUCTION_RUNTIME_PROMOTION=NOT_AUTHORIZED`. `COUNTERFACTUAL_EFFECTIVENESS_GATE=SUPERSEDED_AS_INVALID_PREREQUISITE`; `EXPERIMENTAL_RUNTIME_ADMISSION=PASS`.

## Implementation

- Added explicit debug selector `controlPolicy=V2|V3`; V2 remains the load/reload default and the choice is not persisted.
- V2 and V3 consume the same accepted perception/semantic measurement stream; only the controller differs.
- Added the V3 runtime without predictive STOP, BRAKING, motion-tracking substates, dynamic axis arbitration, Provider, Backend, or Luna.
- User copy is one small step: 往左一点、往右一点、靠近一点、退后一点. The UI explicitly says to make one adjustment and naturally stop.
- Added a simple V3 tester panel: policy, stage, framing, X, measurement quality, stability, action, outcome, and controller latency.
- Extended scalar-only V3 trace with issue, movement, meaningful motion, settle, measurement, outcome times; start/settled versions, relations, errors; quality, freshness, duration, stage, action, and outcome.
- `HumanSettleDetectorV01` remains a candidate at 375 ms stable / 4500 ms timeout. Controller episode states remain ISSUED → WAIT_FOR_SETTLE → EVALUATED.

## Verification

- Automated tests: 229/229 PASS.
- TypeScript: PASS.
- Production build: PASS / 35 modules.
- V2 runtime regression: PASS; existing full tests plus browser `ready-after-success`, READY true, zero console errors.
- V3 browser gate: PASS / 10 of 10 required deterministic scenarios.
- Default V2 and session-only V3 selection: browser verified.
- Raw Video Upload, Backend per-frame, Provider, Luna: 0.

## Device gate

Fresh OPPO V3 FRAMING_ONLY, X_ONLY, COMBINED, and ALREADY_SATISFIED trials are not yet available. Accordingly, the V3 experimental device gate and all prospective human effectiveness metrics remain `NOT_REACHED`; V3 is not promoted and no historical V2 percentage is reused.

Next authorized action within this same task is the small OPPO exploratory Gate documented in `evidence/control-v3/prospective-human-step.md`. A prospective V2/V3 comparison is allowed only after all four V3 scenarios prove viable. V3 Gate 2 remains `NOT_DEFINED`.
