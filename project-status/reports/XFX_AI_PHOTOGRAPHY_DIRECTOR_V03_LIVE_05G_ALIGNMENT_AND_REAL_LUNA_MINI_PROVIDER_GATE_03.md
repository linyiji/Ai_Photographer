# XFX AI Photography Director V0.3 Live 05G Alignment and Real Luna Mini Provider Gate 03

## Result

```text
TASK_RESULT = MANUAL_REVIEW_REQUIRED

DIRECTOR_V03_CONTRACT_ALIGNMENT = PASS
LIVE_05G_ALIGNMENT = PASS
LIVE_CAPABILITY_CATALOG = PASS
SUPPORTED_FRAMING_PROFILES = HEAD / HEAD_SHOULDERS / UPPER_BODY / THREE_QUARTER / FULL_BODY
HEAD_SHOULDERS_IMPLIES_HIPS = NO
DIRECTOR_LIVE_PRIVATE_MEASUREMENT_LEAK = 0

PHOTOGRAPHY_DIRECTOR_INPUT = V02_CREATED
SHOT_PLAN_CANDIDATE = V02_CREATED
LIVE_TARGET_BLUEPRINT = V02_CREATED

SUBJECT_UNDERSTANDING = PASS_WITH_WARNING
SCENE_UNDERSTANDING = PASS_WITH_WARNING
LIGHTING_UNDERSTANDING = PASS_WITH_WARNING
SHOT_PLAN_CANDIDATES = PASS_WITH_WARNING
MEANINGFUL_THREE_PLAN_DIVERSITY = PASS_WITH_WARNING
SUBJECT_SCENE_CROSS_REASONING = PASS_WITH_WARNING
LIVE_EXECUTABILITY = PASS
REALITY_FIDELITY = NOT_EXERCISED

REAL_PROVIDER = NOT_EXERCISED
REAL_PROVIDER_CALLS = 0
LOGICAL_MODEL_ALIAS = gpt-luna
PROVIDER = NOT_CONFIGURED
PROVIDER_MODEL = NOT_CONFIGURED
PROVIDER_IMAGE_INPUT = NO
PROVIDER_IMAGE_INPUT_COUNT = 0
PROVIDER_RAW_VIDEO = 0
PROVIDER_FRAME_STREAM = 0
GEOMETRY_FRAME_SEQUENCE_SENT = 0
SCENE_SPATIAL_PRIVATE_STATE_SENT = 0

MINI_GATE_CASE_1 = NOT_RUN
MINI_GATE_CASE_2 = NOT_RUN
MINI_GATE_CASE_3 = NOT_RUN
LEVEL1_WITHOUT_SPATIAL = PASS
INSUFFICIENT_SPATIAL = PASS
USABLE_SPATIAL_REAL_CASE = SOURCE_REQUIRED

DIRECTOR_LATENCY_CASE_1 = NOT_EXERCISED
DIRECTOR_LATENCY_CASE_2 = NOT_EXERCISED
DIRECTOR_LATENCY_CASE_3 = NOT_EXERCISED
PROVIDER_USAGE = SOURCE_REQUIRED
PROVIDER_COST = SOURCE_REQUIRED

HUMAN_PHOTOGRAPHY_REVIEW = PENDING
AI_PROVIDER_MINI_GATE = MANUAL_REVIEW_REQUIRED
AI_DIRECTOR_PRODUCTION = NOT_YET_PROMOTED
NEXT_LARGER_EVALUATION = NOT_AUTHORIZED

POST_CAPTURE_AI = NOT_STARTED
GPT_IMAGE_2 = NOT_STARTED
MAIN_INTEGRATION = NOT_STARTED

AUTOMATED_FOCUSED_TESTS = PASS (71/71)
FULL_BACKEND_REGRESSION = PASS (192/192)
```

## Source and Git safety

The Director track began clean and synchronized at accepted V0.2 commit `fdfd6de9b35a5ced50273b49deb04e02ec0ad287`. The AI I/O source was read-only at accepted commit `da1d80770f1d48714c579350b92121190fabedef`.

The Live worktree had legitimately advanced from specified 05G commit `8dfe92b272efd0c0bc5785d6cf2e102cf21f6d75` to 05H documentation commit `dd476c6edc54bad6526c51eb5109f1eacf38bf1f`. The 05G commit is an ancestor. The Live worktree also contained unrelated user-owned untracked voice files; none was opened, modified, staged or removed. Live 05G was read only from the specified commit object.

## Contract alignment

V0.1's `ENVIRONMENTAL_FULL_BODY`, `THREE_QUARTER_LIFESTYLE`, and `CLOSE_EMOTIONAL_PORTRAIT` values are artistic concepts and cannot unambiguously represent all five product-level Live 05G profiles. The frozen V01 contracts were therefore preserved and V02 versions were added for Director input, ShotPlan and LiveTarget blueprint.

`LiveCapabilityCatalogV01` exposes only product capability: five framing profiles, target zones, normalized placement support, axis relations, current coarse orientation execution false, detailed gesture false, and device admission `PENDING_05H`. The 05G future-scope document says coarse orientation may be added earlier than detailed gesture, but the specified 05G code has no executable orientation target; V0.3 therefore keeps it optional and non-blocking. The catalog contains no landmark, reducer, crop-classifier, smoothing, readiness, timing, hysteresis or direction-mapper implementation.

`HEAD_SHOULDERS` is explicitly head plus shoulders and does not imply hips. `UPPER_BODY` is the separate head-to-hip product profile. Contract tests cover all five profiles; `LEFT_TOP`, `CENTER`, `RIGHT_BOTTOM`; generalized normalized placement; optional-only detailed gesture; and zero private measurement leakage.

## Context, candidate and AI I/O governance

`PhotographyDirectorInputV02` wraps already validated V01 context and adds the catalog, explicit `STRUCTURED_ONLY`/`STRUCTURED_PLUS_IMAGES` mode and authorized visual refs. Image mode permits no more than one subject reference and one to three selected scene images. Raw video, frame streams, geometry sequences and private Scene Spatial state are structurally excluded.

`ShotPlanCandidateV02` carries explicit framing, representable placement, bounded pose requirements, camera relationship, lighting use, accepted scene elements, compact cross-reasoned rationale and product-level `LiveTargetBlueprintV02`. Unknown View, anchor, evidence, scene element, zone or framing; required detailed gesture; P3 claims; private Live details; missing cross-reasoning; weak diversity; and missing blueprint all fail closed.

The V0.3 track selectively migrates accepted AI I/O idempotency, latest-input supersession, current session revision, one bounded retry and promotion-time staleness semantics. A completed result cannot be promoted after a newer same-revision input becomes current.

## Deterministic evaluation

V0.1, V0.2 and V0.3 focused regression passes 71/71; the full backend passes 192/192. Four controlled spatial shapes—absent, partial, insufficient and usable—remain valid at the product contract layer. Three sanitized deterministic case shapes each produce three candidates with variation in framing, placement and scene/view use. These are Fake/Replay contract evidence, not real provider quality evidence.

Subject, Scene, Lighting, candidate quality, diversity and cross-reasoning are therefore `PASS_WITH_WARNING`: deterministic gates pass, but no real provider or human photography review ran. Live executability is `PASS` at the product-contract boundary only; device admission remains a separate 05H gate.

## Real gpt-luna Mini Gate stop

The safe runtime preflight found no explicit provider/model configuration. Consequently no configured credential indirection, image-input capability, structured-output capability, service tier, usage/cost reporting or model snapshot was available. The Director package also contains only a provider-neutral Protocol and Replay transport, not a real transport.

The existing media inventory explicitly records `provider_send_authorized = false`. No exact Subject or Scene asset has Owner authorization for provider send, and the available subject inventory lacks the requested clothing/style diversity. These independent blockers require stopping before transport.

No model was substituted, no credential was invented or inspected, and no authorization was inferred. All three real cases remain `NOT_RUN`; provider image count and real provider call count remain zero. Since real output does not exist, Reality Fidelity is `NOT_EXERCISED`, latency is `NOT_EXERCISED`, and human review is `PENDING`.

## Next authority required

To resume the Mini Gate, the Owner must provide all of: explicit provider and exact model configuration for logical alias `gpt-luna`; approved secret indirection; confirmed structured-output and image-input support; a reviewed real transport; explicit provider-send authorization for the exact three-case media; and an authorized human photography reviewer. A larger evaluation, production promotion, Main integration and post-capture AI remain unauthorized.
