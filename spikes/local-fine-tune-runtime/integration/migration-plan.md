# Selective migration plan

Do not merge Spike history into Main.

Future `XFX_LOCAL_FINE_TUNE_INTEGRATION_01` starts from the then-current accepted Main checkpoint and selectively adapts:

1. generated M01 projections and validation;
2. provider-neutral runtime/session interfaces;
3. deterministic parameter kernel and safe mappings;
4. mask adapter/validation and controlled unavailable behavior;
5. latest-state preview scheduler and source invalidation;
6. Worker final renderer with Canvas2D fallback;
7. P12 UI mapping without Spike debug/test controls;
8. Recipe/asset/MyFinalPhoto persistence and governed events;
9. M03 replay scenarios plus H5/WeChat/OPPO gates.

Spike-only fixture providers, benchmark panel, synthetic asset ids, localStorage persistence, direct download behavior and temporary tunnel workflow must not ship as production adapters.

Before migration, freeze a Main integration acceptance commit and resolve only blocking contract gaps through explicit Authority tasks. Main M06 is currently active; integration waits for a clean accepted checkpoint.
