# Scene Spatial Design Authority V0.2

V0.2 supersedes conflicting P2/P3 judgment semantics in V0.1. V0.1 remains historical design evidence and is not deleted.

Preserved principles: Reality First, Candidate != Authority, one Scene Scan, accepted P0/P1 behavior, Region Count != Candidate Count, maximum three ViewCandidates, and Main Integration `NOT_STARTED`.

Canonical chain:

```text
P0 SCAN → P1 VIEW → P2 GEOMETRY → future P3 PHOTOGRAPHY AFFORDANCE → future AI PHOTOGRAPHY DIRECTOR
```

Client `SpatialPrecheckV01` is `ROUTING_HINT_ONLY`. Only the first-party Production Backend Geometry Solver may emit current-session `SpatialEvidenceV02.status`. Controlled Reference is algorithm-validation authority only.

`CompositionAnchorCandidateV01` is an image-plane LEFT/CENTER/RIGHT anchor. It is never a physical `SubjectPlacementCandidate`.
