# Main Mapping (Future Only)

Potential call site: Golden Flow Step 02 `UNDERSTAND REALITY`, before Target candidate generation. Main would launch a production-owned sweep capability, validate its manifest, turn ordered transient keyframes into governed asset candidates only with explicit retention consent, and produce `SceneSpatialContext` for Photography Director / Scene Understanding.

No Main source is changed and no spike UI becomes product navigation in P0.
