# Selective Migration Plan

1. Freeze an accepted Scene Spatial head and interface revision.
2. Re-evaluate production platform contracts and privacy/retention consent.
3. Copy only accepted orientation, coverage, sampler, manifest, and YawMap modules into Main-owned capability paths.
4. Replace transient references with governed Main adapters; preserve zero per-frame backend/provider calls.
5. Add Main contract, workflow, device, and regression acceptance.

Do not raw-merge the spike and do not introduce a cross-worktree package or filesystem dependency.
