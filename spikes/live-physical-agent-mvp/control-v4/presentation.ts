import { V4_ACTION_COPY } from './controller.js';
import type { V4Action, V4Snapshot } from './types.js';
export type V4PresentationName='DISARMED'|'ACQUIRING'|'ACTION_REQUIRED'|'WAITING_FOR_RESPONSE'|'WAITING_FOR_SETTLE'|'VERIFYING_READY'|'READY';
export interface V4Presentation {state:V4PresentationName;primary_copy_zh:string;overlay_copy_zh:string;action:V4Action|null}
const result=(state:V4PresentationName,text:string,action:V4Action|null=null,overlay=text):Readonly<V4Presentation>=>Object.freeze({state,primary_copy_zh:text,overlay_copy_zh:overlay,action});
export function deriveV4Presentation(snapshot:V4Snapshot|null):Readonly<V4Presentation>{if(!snapshot?.armed)return result('DISARMED','准备完成，点击开始 V4 引导');if(snapshot.ready)return result('READY','好，就这里');if(snapshot.action)return result('ACTION_REQUIRED',V4_ACTION_COPY[snapshot.action],snapshot.action);if(snapshot.active_episode){if(snapshot.active_episode.response_observed)return result('WAITING_FOR_SETTLE','已检测到移动，请自然停下');return result('WAITING_FOR_RESPONSE',snapshot.reminder_due?'还没有检测到移动，请按刚才提示移动一小步':'按刚才提示移动一小步，然后自然停下',snapshot.active_episode.action,V4_ACTION_COPY[snapshot.active_episode.action]);}if(snapshot.acquisition_copy_zh)return result('ACQUIRING',snapshot.acquisition_copy_zh);const required=snapshot.target.ready_stable_ms;return snapshot.observation.stable&&snapshot.observation.quality==='GOOD'?result('VERIFYING_READY',`位置合适，稳定确认 ${Math.min(snapshot.ready_hold_elapsed_ms,required).toFixed(0)} / ${required} ms`):result('VERIFYING_READY','位置已满足，检测到轻微移动，请停稳');}
